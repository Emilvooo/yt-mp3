services:
  yt-mp3:
    build: .
    container_name: yt-mp3
    restart: unless-stopped
    env_file:
      - .env
    environment:
      YTDLP_FORCE_IPV6: "1"
    network_mode: host
    command: uvicorn main:app --host 127.0.0.1 --port 8910
